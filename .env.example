# Optional for generative answers. Without this key, the app still runs in conservative extractive-fallback mode.
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile

# Retrieval thresholds can be tuned against eval/eval_dataset.json
MIN_SIMILARITY=0.34
MIN_CONFIDENCE=0.58
TOP_K=5
